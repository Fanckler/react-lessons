import React from 'react';

interface IProps {
    children: any
}

export const OpenLoginPopup:React.FC<IProps> = (props) => {
    const {children} = props;
    return (
        <>
        </>
    );
}


